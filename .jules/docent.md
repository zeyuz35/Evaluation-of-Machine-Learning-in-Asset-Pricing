## 2024-05-14 - Replace informal terms
**Learning:** Found instances of informal/unprofessional language ("stupid", "annoying", "dumb") in comments in multiple files. This is a common pattern in the codebase.
**Action:** Remove or replace them with more professional terminology.

## 2024-05-14 - Replace print/cat with message
**Learning:** R code contains `cat()` and `print()` in demo files inside `R/xgboost/demo`. However, demo scripts often use print/cat for terminal output natively, making it a stylistic gray area. I will instead focus on the informal language in comments.
**Action:** Focus on cleaning up comments first.
