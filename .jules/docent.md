## 2025-02-23 - Replacing informal language and raw `cat()` calls with professional standards
**Learning:** Found several scripts containing informal language contractions ("don't", "can't", "doesn't", etc.) and using `cat()` for structured output and progress printing rather than base logging mechanisms like `message()`, particularly in `.Rmd` documents in `R/empirical/` and `R/xgboost/demo/`. The `agents.md` strictly requires professional tone/ASCII formatting, avoiding emoji, and replacing raw `cat()` output for informational messages/progress printing.
**Action:** Use `sed` or R-based automated replacement to enforce a professional tone (removing contractions) and convert logging/informational calls from `cat()` to `message()`, ensuring `appendLF = FALSE` is properly set for progress indicators.
