## 2026-05-13 - Optimize bind_rt_predictor by replacing iterative rbind with lapply\n**Learning:** The function `bind_rt_predictor` in `R/simulation/Simulation.Rmd` appends rows to a data.frame inside a `for` loop over time periods, causing an O(N^2) 'growing objects' performance bottleneck.\n**Action:** Use `lapply()` to accumulate a list of data.frames per time period and combine them once at the end using `do.call(rbind, ...)`.
