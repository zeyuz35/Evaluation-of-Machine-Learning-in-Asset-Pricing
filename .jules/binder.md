## 2024-05-24 - Identifying lack of proper R structure for a package
**Learning:** This repository has a mixture of R package and analysis script structures. `agents.md` specifies that code should move towards package structure where `R/` directory shouldn't have `library()` or `require()` calls and instead rely on proper R namespace descriptions. There are multiple `.Rmd` files in `R/` and `library()` calls everywhere.
**Action:** Remove `library()` and use explicit namespaces `pkg::function()` in `R/umap_play.R` to respect the memory protocol and Universal agents. Wait, the `umap_play.R` script is inside the `R/` directory where `library()` is prohibited. By modifying it to use explicit namespaces, I improve codebase hygiene and standards compliance without breaking functionality.
